#include <stdio.h>
int getMin(int a, int b, int c) {
	
	int min = a;
	if(min > b)
	min = b;
	if(min > c)
	min = c;
	
	return min;
}
int main(void) {
	
	int a, b, c;
	scanf("%d %d %d", &a, &b, &c);
	int min = getMin(a, b, c);
	printf("%d", min);
	
	return 0;
}
